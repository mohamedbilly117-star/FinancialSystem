{
  "ConnectionStrings": {
    "//NOTE": "Offline/LAN deployment (Prompt 0 & Prompt 3): a named local SQL Server Express instance, Windows (Trusted) authentication - no SQL login/password to manage, consistent with 'No Cloud Dependency'. Replace 'SQLEXPRESS' / server name for the actual production LAN server at deployment time; this value must never be committed with a real production server name or credentials (see .gitignore: appsettings.*.local.json).",
    "DefaultConnection": "Server=.\\SQLEXPRESS;Database=GovERP;Trusted_Connection=True;TrustServerCertificate=True;MultipleActiveResultSets=true;Encrypt=True"
  },

  "Logging": {
    "//NOTE": "Serilog (configured in code via SerilogConfiguration.cs) is the authoritative logging pipeline. This section governs the small amount of framework-internal logging that flows through Microsoft.Extensions.Logging before Serilog's provider takes over, and is kept minimal deliberately.",
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      "Microsoft.EntityFrameworkCore": "Warning"
    }
  },

  "AllowedHosts": "*",

  "GovErp": {
    "//NOTE": "Application-specific settings that are NOT accounting/workflow/distribution business rules (those live in configuration tables per Prompt 11 - System Configuration - not in appsettings.json, since they must be editable by administrators at runtime without a redeploy).",
    "Organization": {
      "NameAr": "REPLACE_WITH_ORGANIZATION_NAME_AR",
      "NameEn": "REPLACE_WITH_ORGANIZATION_NAME_EN"
    },
    "DefaultCulture": "ar-EG"
  }
}
